# E2E 테스트 가이드

이 프로젝트는 Playwright를 사용하여 E2E (End-to-End) 테스트를 수행합니다.

## 📁 테스트 파일 구조

```
e2e/
├── event-creation.spec.ts      # 일정 생성 테스트
├── event-view.spec.ts           # 일정 조회 및 뷰 전환 테스트
├── event-search.spec.ts         # 일정 검색 테스트
├── event-edit-delete.spec.ts    # 일정 수정 및 삭제 테스트
├── recurring-event.spec.ts      # 반복 일정 테스트
└── event-overlap.spec.ts        # 일정 충돌 감지 테스트
```

## 🧪 테스트 시나리오

### 1. 일정 생성 (event-creation.spec.ts)
- ✅ 단일 일정 생성
- ✅ 반복 일정 생성 (매일, 주간, 월간, 연간)
- ✅ 필수 입력값 검증
- ✅ 시간 유효성 검증
- ✅ 알림 시간 설정

### 2. 일정 조회 및 뷰 전환 (event-view.spec.ts)
- ✅ 월별 뷰 표시
- ✅ 주별 뷰 전환
- ✅ 일별 뷰 전환
- ✅ 달력 내비게이션 (이전/다음/오늘)

### 3. 일정 검색 (event-search.spec.ts)
- ✅ 제목으로 일정 검색
- ✅ 검색어 지우기
- ✅ 검색 결과 없음 표시

### 4. 일정 수정 및 삭제 (event-edit-delete.spec.ts)
- ✅ 단일 일정 수정
- ✅ 단일 일정 삭제

### 5. 반복 일정 (recurring-event.spec.ts)
- ✅ 반복 일정 수정 다이얼로그 표시
- ✅ 단일 인스턴스만 수정
- ✅ 모든 인스턴스 수정
- ✅ 반복 일정 삭제 다이얼로그 표시
- ✅ 단일 인스턴스만 삭제
- ✅ 모든 인스턴스 삭제

### 6. 일정 충돌 감지 (event-overlap.spec.ts)
- ✅ 겹치는 시간대 경고 다이얼로그
- ✅ 충돌 경고에서 계속 진행
- ✅ 충돌 경고에서 취소

## 🚀 테스트 실행 방법

### 전체 테스트 실행 (헤드리스 모드)
```bash
pnpm test:e2e
```

### UI 모드로 테스트 실행
```bash
pnpm test:e2e:ui
```

### 브라우저를 보면서 테스트 (헤드 모드)
```bash
pnpm test:e2e:headed
```

### 디버그 모드로 테스트 실행
```bash
pnpm test:e2e:debug
```

### 특정 테스트 파일만 실행
```bash
pnpm exec playwright test event-creation.spec.ts
```

### 특정 브라우저만 테스트
```bash
# Chromium만
pnpm exec playwright test --project=chromium

# Firefox만
pnpm exec playwright test --project=firefox

# WebKit만
pnpm exec playwright test --project=webkit
```

## 📊 테스트 리포트 확인

테스트 실행 후 HTML 리포트가 자동으로 생성됩니다.

```bash
pnpm exec playwright show-report
```

## 🔧 환경 설정

### playwright.config.ts
- **baseURL**: `http://localhost:5173` (개발 서버 주소)
- **테스트 디렉토리**: `./e2e`
- **브라우저**: Chromium, Firefox, WebKit
- **자동 서버 시작**: 개발 서버가 자동으로 시작됩니다

## 💡 테스트 작성 팁

### 1. 테스트 격리
각 테스트는 독립적으로 실행되어야 합니다. `test.beforeEach`를 사용하여 테스트 전에 필요한 상태를 설정하세요.

```typescript
test.beforeEach(async ({ page }) => {
  await page.goto('/');
  await page.waitForLoadState('networkidle');
});
```

### 2. 선택자 우선순위
1. **역할 기반 선택자** (권장): `page.getByRole('button', { name: '저장' })`
2. **레이블**: `page.getByLabel('제목')`
3. **텍스트**: `page.getByText('일정이 추가되었습니다')`
4. **테스트 ID**: `page.getByTestId('event-submit-button')`

### 3. 대기 시간
네트워크 요청이나 애니메이션을 기다릴 때:
```typescript
await page.waitForLoadState('networkidle');
await page.waitForTimeout(500); // 필요한 경우에만 사용
```

### 4. 단언문 (Assertions)
```typescript
await expect(page.getByText('성공')).toBeVisible();
await expect(page.getByLabel('제목')).toHaveValue('팀 회의');
```

## 🐛 트러블슈팅

### 포트 충돌 문제
개발 서버가 이미 실행 중이라면 `playwright.config.ts`의 `reuseExistingServer: true` 옵션이 기존 서버를 재사용합니다.

### 타임아웃 문제
느린 환경에서는 타임아웃을 늘릴 수 있습니다:
```typescript
test('느린 테스트', async ({ page }) => {
  test.setTimeout(60000); // 60초
  // 테스트 코드...
});
```

### 스크린샷 및 비디오
실패한 테스트의 스크린샷은 자동으로 저장됩니다. 설정에서 비디오 녹화도 활성화할 수 있습니다.

## 📚 참고 자료

- [Playwright 공식 문서](https://playwright.dev/)
- [Playwright Best Practices](https://playwright.dev/docs/best-practices)
- [Testing Library 가이드](https://testing-library.com/)
